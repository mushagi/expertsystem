#include "expertsystem.hpp"
using namespace std;
class Rule{
	public : string rule;
	public : Rule(string rule){
				 this->rule = rule;
			 }
	public : string toString() {
				 return (rule);
			 }
};
